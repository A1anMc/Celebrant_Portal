# Production Environment Variables Example
# Copy this to your Render environment variables

# Application
ENVIRONMENT=production
DEBUG=false

# Database (Render will provide this automatically)
DATABASE_URL=postgresql://user:password@host:port/database

# Security (Generate secure values)
SECRET_KEY=your-super-secure-secret-key-here
ACCESS_TOKEN_EXPIRE_MINUTES=60
REFRESH_TOKEN_EXPIRE_DAYS=30

# CORS Origins (Update with your actual domains)
ALLOWED_ORIGINS=https://melbourne-celebrant-portal.vercel.app,https://amelbournecelebrant.com.au

# Email (Brevo free tier)
SMTP_HOST=smtp-relay.brevo.com
SMTP_PORT=587
SMTP_USERNAME=your-brevo-username
SMTP_PASSWORD=your-brevo-password
FROM_EMAIL=noreply@amelbournecelebrant.com.au

# File Upload
MAX_FILE_SIZE=10485760
UPLOAD_DIR=/tmp/uploads 